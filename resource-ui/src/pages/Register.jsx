import { useEffect,useState } from "react"; import { Link,useNavigate } from "react-router-dom"; import { registerUser } from "../services/authService"; import api from "../services/api";
export default function Register(){const navigate=useNavigate();const [form,setForm]=useState({firstName:"",lastName:"",email:"",password:"",phone:"",role:"STUDENT",institutionId:"",departmentId:""});const [institutions,setInstitutions]=useState([]),[departments,setDepartments]=useState([]),[error,setError]=useState(""),[loading,setLoading]=useState(false);
useEffect(()=>{api.get('/institutions').then(r=>setInstitutions(r.data||[])).catch(()=>{})},[]);useEffect(()=>{if(form.institutionId)api.get(`/departments/institution/${form.institutionId}`).then(r=>setDepartments(r.data||[])).catch(()=>setDepartments([]))},[form.institutionId]);const change=e=>setForm({...form,[e.target.name]:e.target.value});const submit=async e=>{e.preventDefault();setError("");setLoading(true);try{await registerUser(form);navigate('/login')}catch(x){setError(x.message)}finally{setLoading(false)}};
return <div className="auth-shell"><section className="auth-visual register-visual"><div className="brand-mark">RU</div><div><span className="eyebrow">JOIN RESOURCEHUB</span><h1>One workspace for every resource.</h1><p>Create your account and make institutional equipment easier to discover, book and manage.</p><div className="benefit-list"><div><b>01</b><span><strong>Discover</strong><small>Find available equipment quickly.</small></span></div><div><b>02</b><span><strong>Book</strong><small>Plan research time without conflicts.</small></span></div><div><b>03</b><span><strong>Track</strong><small>Stay informed about asset status.</small></span></div></div></div><small>Secure access • Role-based permissions</small></section><section className="auth-panel"><form className="pro-auth-card register-card" onSubmit={submit}><span className="eyebrow dark">GET STARTED</span><h2>Create your account</h2><p className="subtext">Use your institutional details to set up your profile.</p>{error&&<div className="pro-alert">⚠ {error}</div>}<div className="form-grid"><div><label>First name</label><input name="firstName" value={form.firstName} onChange={change} placeholder="First name" required/></div><div><label>Last name</label><input name="lastName" value={form.lastName} onChange={change} placeholder="Last name" required/></div><div className="full"><label>Email address</label><input type="email" name="email" value={form.email} onChange={change} placeholder="name@institution.edu" required/></div><div><label>Phone</label><input name="phone" value={form.phone} onChange={change} placeholder="10-digit mobile number" required/></div><div><label>Role</label><select name="role" value={form.role} onChange={change}><option value="STUDENT">Student</option><option value="FACULTY">Faculty</option><option value="ADMIN">Admin</option></select></div><div className="full"><label>Password</label><input type="password" name="password" value={form.password} onChange={change} placeholder="Minimum 8 characters" minLength="8" required/></div><div><label>Institution</label><select name="institutionId" value={form.institutionId} onChange={change} required><option value="">Select institution</option>{institutions.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></div><div><label>Department</label><select name="departmentId" value={form.departmentId} onChange={change} required><option value="">Select department</option>{departments.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></div></div><button className="primary-action" disabled={loading}>{loading?"Creating account…":"Create account →"}</button><p className="auth-switch">Already registered? <Link to="/login">Sign in</Link></p></form></section></div>}
